import { Router } from 'express';

const router = Router();

// Import and register feature routes here
// Example:
// import didRoutes from './did';
// router.use('/did', didRoutes);

export default router;
